export const PAGE_HEIGHT_PX = 1123;
export const PAGE_WIDTH_PX = 794;
export const MARGIN_LEFT_PX = 53;
export const MARGIN_TOP_PX = 19;
export const CONTENT_WIDTH_PX = PAGE_WIDTH_PX - MARGIN_LEFT_PX * 2;
export const FOOTER_HEIGHT_PX = 60;
export const AVAILABLE_HEIGHT_PX = PAGE_HEIGHT_PX - MARGIN_TOP_PX - FOOTER_HEIGHT_PX;
// Height consumed by the header block (logo + title bar) on the first page,
// including its bottom margin, so the pagination algorithm accounts for it.
export const HEADER_HEIGHT_PX = 82;

export const COLOR_YELLOW = "#F5B800";
export const COLOR_ORANGE = "#D97706";
export const COLOR_DARK_GRAY = "#374151";
