# poc-js-pdf

Small React + TypeScript sample app using `jspdf`.

## Run locally

```bash
npm install
npm run dev
```

Open the app in your browser and click `Generate PDF`.

The generated PDF opens in a new tab and includes:

- A title
- A local image stored inside the project at `src/assets/hero.png`
